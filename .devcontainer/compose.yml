---
services:
  devcontainer-base-images:
    container_name: devcontainer-base-images
    build:
      context: .
    volumes:
      - ..:/home/vscode/base-images:cached
    user: vscode
    tty: true
